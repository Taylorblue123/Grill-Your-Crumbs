单文件交互原型：`grill-demo.html`。双击就开，无后端、无依赖、无网络。

- **自动演示 ▶** 从落地页一路跑到工作区（约 80 秒），中途演示面板收放、账本换栏目、候补拖进简历、材料拖出本场。Esc 随时接管。
- 右上角 **◐** 切深色 / 浅色，整套系统一起换。
- 工作台快捷键：`1` `2` `3` `4` 收放四个面板，`⌘Z` 撤回，输入框 `↵` 发送。
- 跳到任意状态：
  `#screen=wb&round=3&way=pick&ledger=tag&panel=crumbs:min,draft:max&promote=h10&drop=c4&theme=dark&saved=1`

---

## 五屏

**⓪ 落地页 → ① 工作区 → ② 投喂 → ③ 工作台（四面板）→ ④ 成果 → 回 ①**

---

## A-1 · 两种投喂方式

投喂页顶部是一个二选一：

**方式 A｜我有一段想改的经历** — 粘一段自己写的，它成为最后对比用的基准。（原来的路径）

**方式 B｜从我的材料里挑一段** — **一个字都不用写**。系统先把 12 条 crumbs 聚成几段「能讲的经历」，每段给出：

- 它是**聚自哪几条材料**（材料名直接列出来，可核对）
- **材料厚 / 中 / 薄**，以及缺口在哪（「6 条材料里有代码、有事故、有吵架记录，却没有一条写了结局」）
- 预计能问出几轮

选中之后会明确告诉你：**这段基准是从材料里拼的，不是你写的**。所以成果页的对比口径也跟着换——不是「你写的 vs 成稿」，而是「**材料里本来有的 vs 问出来之后的**」，BEFORE 栏直接列出那几条材料原文。

> 原型里只有第一段有拷问脚本（6 轮问答是手写假数据），另外两段选中后会禁用「开始」并说明原因，不假装能跑。

## A-2 / A-3 · 工作台改成四面板

顺序就是你要的：**材料 — 拷问 — 简历活稿 — 收获账本**。

**每个面板三态**：正常 / 收成边上的竖条（带竖排标签和计数）/ ⤢ 放大顶开其他。顶部有一条**面板条**，四个开关一字排开，显示当前状态；面板条自己也能收起（▲）。窄屏会按优先级自动收，但你手动点过的面板不会被自动改。

**材料面板独立出来了**，分两组：**本场使用中** / **材料库·没进本场**。卡片可以在两组之间**拖**，也能点 ±。

这里加了一个原来没有的因果：**把材料拖出本场，引用它的句子当场变红并标「出处已移出」**，计数从「有材料出处」挪到「无出处」。这不是装饰——它和后端 schema 里 `source_needs_crumb` 那条约束是同一件事：出处没了，那句话就不再算有出处。

**收获账本和 Thread 成果合并了**，就在最右侧一个面板里。你说得对，它们本来就是同一份数据，而且旧的 Thread 视图把左边已经显示过的问题又抄了一遍。

现在的账本：

- **栏目 = matrices**，可切 **按维度** / **按标签**。按维度是固定六栏（空栏保留，因为「这场没问到这个角度」本身是信息）；按标签是动态栏目（`#数字` `#取舍` `#冲突` `#业务指标`…），只出现有内容的栏。
- 每条只标 **「第 N 轮 ↗」**，**不复述问题**。点它会展开左边的拷问面板并把那一轮滚动到视野中央、高亮一下。
- 候补条目是金色虚线卡片，可直接拖进简历面板。

## B · Dashboard 重做

你的直觉是对的，我认为原因是：**「拷问记录 / 事实库 / artifact / 候补」是我们的数据模型，不是用户的心智。** 用户不会想「我有 3 个 thread 和 24 条 fact」，他想的是「我有几段能拿去讲的经历，哪段够厚了，哪段还很薄」。

所以这一版按**经历**组织。每张卡是一段经历：

- **六格维度矩阵** —— 填了几格是数出来的（每格是该维度下的事实条数），空格＝这段还没被问到那个角度。仍然没有百分比。
- 一行可数指标：材料 N 条 · 事实 N 条 · 维度 N/6 · 问过 N 轮
- 它**喂了哪几份稿子**（没有就明说「还没喂给任何一份稿子」）
- 展开可以看这段的全部事实，按维度分组

然后两条次级货架：**产出物**（每份标明由哪几段经历喂出来 + 三色出处条）和**材料**（原料层，同一条材料可以喂多段经历）。

对你那几个"什么才算资产"的问题，我的判断是：

- **经历是资产** —— 它是用户能带走、能复用、能一直加厚的东西。
- **产出物是快照** —— 简历 v3 是某个时点从经历里取的料，它会过期。
- **材料是原料** —— 是输入，不是成果。
- **拷问记录不是资产，是过程** —— 它退成了经历卡上的「问过 N 轮」和每条事实的「第 N 轮 ↗」。单独给它一个顶层分类，就等于让用户管理我们的日志。
- **事实库确实不该是顶层分类** —— 你的犹豫是对的。一条孤零零的事实没有意义，它只有挂在某段经历下才有意义。所以事实降级成经历卡里的内容。

**候补也不再是分类，而是待办。** 顶部一条待办栏：「N 条候补事实还没进任何稿子 · N 段经历问到一半」，带直达按钮。名词按心智分（经历/产出物/材料），动作按待办列——这是这一版的组织原则。

> 这仍然是一个提案。如果你觉得「经历」这个单位太重（比如有人就是想改一条 bullet，不想先建一段经历），另一条路是**以产出物为中心**：Dashboard 就是几份简历，事实和经历都藏在简历下面。我没选它，是因为那样事实就很难跨简历复用——而复用是这个产品最值钱的部分。

---

## 已验证

- **46 条新断言 + 21 条回归断言**在无头 Chrome 全绿：四面板顺序与三态（用实际渲染宽度断言，不是 CSS 声明）、Thread 面板确实已删、材料拖出后 orphan 计数迁移、账本按维度/按标签两种分栏、账本不含问题原文、方式 B 的候选经历与 BEFORE 口径切换、Dashboard 经历卡与维度矩阵、存档后产出物挂到经历下。全程 0 个 JS 错误。
- 踩到并修掉一个坑：`.panels` 上原本有 `transition: grid-template-columns`。**Safari / Firefox 根本不动画这个属性**，而 Chromium 里它会让「面板现在多宽」在动画期间读不出来（我的第一版断言就是被它骗过去的）。改成宽度直接吸附 + 内容淡入，跨浏览器一致，也可测。注释留在 CSS 里。

## 已知未做

- 面板宽度只能三态切换，**不能拖动分隔条自由调宽**。要做的话得加 splitter 和持久化，是独立一轮的工作量。
- 方式 B 只有第一段经历有拷问脚本。
- 11px 次要说明文字在浅色下对比度 4.2:1（深色 5.6:1 达标），仍是唯一未达 AA 的项。

---

# 这一版新增（Target ＋ 机会）

## 「FOR X」拆成两个字段

之前那四个 chip 其实是**产出物类型**，贴 JD 是**目标**，两件事正交。现在投喂页是：

- **① 产出什么**（格式）：简历 bullet / LinkedIn About / 60 秒自我介绍 / 项目页
- **② 为谁做**（Target，可留空）：从收藏的机会里挑 / 贴一段 JD / 不设目标

好处：**事实库跨 Target 复用**。同一段经历挖过一次，对第二个 JD 只补差额。

## JD 摊开成一张要求清单，不是一个匹配分

贴完 JD 不直接进拷问，先拆成 14 条要求，每条四种状态：

| 状态 | 含义 | 视觉 |
|---|---|---|
| **对上** | 已有材料证据，或依赖的事实已挖到 | 蓝色实心 ✓ |
| **只有弱证据** | 沾边，但差一截（明说差在哪） | 金色 ◐ |
| **还没有** | 材料里 0 条，但**问得出来** | 灰色虚线 ○ |
| **你确实没有** | 问不出来，是真缺口 | 红色 ✕ |

初始 `对上 3 · 弱 3 · 能问出 6 · 确实没有 2`；六轮答完变成 `对上 12 · 确实没有 2`。**没有百分比。**

## 一条红线：JD 是检查表，不是模板

`gap` 状态的要求**永远不会被写成简历文案**。成果页专门有一块：

> **这份稿子对上了 12 / 14 条。** 剩下 2 条是你确实没有的：「熟悉 Kubernetes / 容器化部署」「有消息队列（Kafka / Pulsar）使用经验」。
> 我们**没有替你圆**这几条——市面上按 JD 改简历的工具会给你编一句出来。把它留在这儿，是因为你需要知道自己真正缺什么：那是去补技能的信号，不是去补文案的信号。

## 提问预算 4 : 2

6 轮里 4 轮由 JD 缺口驱动、2 轮打「只有你有」的通用维度。每张问题卡上标 `JD 缺口驱动` 或 `通用维度`，卡片下方一条预算条画出六轮各是哪一类。通用那两轮会明说：「只盯着 JD 会把你身上最独特的部分漏掉。」

## 布局：常驻 Target 条 ＋ 默认收起的第五面板

- 工作台顶部一条 **Target 条**常驻：`目标：后端开发实习生 · 某大厂 | 对上 3 · 弱 3 · 还能问出 6 · 确实没有 2 | 看清单 →`，四个数字都能点，跳到清单对应分组。
- 第五个面板「**目标 JD**」放完整清单，**默认收成 46px 竖条**——上下文由顶部那条负责，面板只在需要和简历并排比对时展开（快捷键 `5`）。

## 简历上的 JD 对齐标记

每条 bullet 后面挂 `↳ JD #3` 小 chip，悬停看是哪条要求。**这是和三色出处正交的第二个轴**：三色回答「这句可信吗」，JD chip 回答「这句有用吗」。刻意没做成第四种颜色，否则和 provenance 打架。

## 机会页（顶层导航第二项）

顶层导航现在是 `工作区 | 机会 | 首页`。机会页三块：

1. **岗位 / 机会** —— 实习、RA 两张真卡（各带自己的要求统计），加一张**合伙人入口卡**（按你说的先留入口，卡上说明：实习/RA 是不对称匹配，合伙人是对称匹配，走的不是同一条路）。「去补缺口」＝开一场以该目标为锚的拷问，**第一部分和第二部分在这里合流**。
2. **同频的人**（placeholder，3 张，顶上标「示意 · 数据虚构」）—— 每张写清**为什么给你看他**：重叠了哪几个标签、他公开的哪一条事实、你对应的哪一条。不是「猜你喜欢」。
3. **可见性**（placeholder，三层）—— 材料原文「永不参与匹配」（锁死）/ 标签签名「参与匹配」（默认开）/ 事实原文「逐条手动放行 · 现在 0 条」（默认关）。页面上明说：**这层没做完，社交那块就不该上线**。

---

## 验证（这一版）

- **45 条新断言**全绿：五面板顺序与目标面板默认收起、Target 条数字与 tally 一致、4:2 预算标记、答题后要求从 ○ 翻 ✓、gap 永远补不上、`↳ JD #n` 没有变成第四种颜色、**无证据要求没有被写进成稿文案**、不设目标时 JD 相关 UI 全部消失、机会页三块、可见性默认状态。
- **另外 67 条回归断言**（46 + 21）继续全绿：四面板三态、材料拖进拖出与 orphan 迁移、账本两种分栏、候补拖进简历、打字/发送时序、三种撤回、半途出稿变薄。
- 合计 **112 条**，0 个 JS 错误。
- 修掉一个真 bug：换 Target 时没有重挂稿子，会残留上一个 JD 的 `↳ JD #n` 标记。现在所有换目标的路径都走同一个 `applyTarget()`。
